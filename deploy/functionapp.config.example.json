{
  // Globally unique name for function app:
  "FunctionAppName": "<yourvalue>",
  // Resource group name for function app:
  "FunctionAppResourceGroupName": "<yourvalue>",
  // Globally unique name for key vault containing secrets:
  "KeyVaultName": "<yourvalue>",
  // Resource group name for key vault:
  "KeyVaultResourceGroupName": "<yourvalue>",
  // Azure region:
  "Location": "westeurope",
  // Get by rightclicking a channel in Discord and copy the link:
  "DiscordWebhookUri": "<yourvalue>",
  // Get by creating an application in Twitch:
  "TwitchClientId": "<yourvalue>",
  // Get by creating an application in Twitch:
  "TwitchClientSecret": "<yourvalue>",
  // Random string used to authenticate callback requests from Twitch:
  "TwitchSignatureSecret": "<yourvalue>",
  // Get by creating an application in Twitter:
  "TwitterAccessToken": "<yourvalue>",
  // Get by creating an application in Twitter:
  "TwitterAccessTokenSecret": "<yourvalue>",
  // Get by creating an application in Twitter:
  "TwitterConsumerKey": "<yourvalue>",
  // Get by creating an application in Twitter:
  "TwitterConsumerSecret": "<yourvalue>",
  // Discord template for new followers:
  "DiscordTemplateOnFollow": "{follower} is now following {streamer} since {followedat}!{newline}Go check them out at {streamuri}",
  // Discord tempalte for when a streamer go live:
  "DiscordTemplateOnStreamOnline": "Live now on Twitch!{newline}{streamer} is streaming{newline}{newline}Join them live at: {streamUri}{newline}",
  // Twitter template for new followers:
  "TwitterTemplateOnFollow": "{follower} is now following {streamer} since {followedat}!{newline}Go check them out at {streamuri}",
  // Twitter template for when a streamer go live:
  "TwitterTemplateOnStreamOnline": "Live now on Twitch!{newline}{newline}{streamer} is streaming{newline}{newline}Join them live at: {streamUri}{newline}{newline}",
  "CallBackUri": "<filled automatically>",
  "FunctionAccessKey": "<filled automatically>",
  "StorageAccountName": "<filled automatically>",
  "KeyVaultResourceId": "<filled automatically>",
  "FunctionAppResourceId": "<filled automatically>"
}
